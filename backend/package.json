{
  "name": "task-manager-serverless-backend",
  "version": "1.0.0",
  "description": "Serverless Task Manager backend on AWS",
  "type": "module",
  "scripts": {
    "build": "npm install",
    "package": "mkdir -p dist && zip -r dist/task-manager-backend.zip package.json package-lock.json src node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  }
}
